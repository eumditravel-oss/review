import type { RebarItem } from "../types/rebar";

type Props = {
  item: RebarItem;
};

export function RebarCardViewer({ item }: Props) {
  return (
    <section className="rebar-card-viewer">
      <header>
        <h2>{item.partName} / {item.dongName} / {item.symbol}</h2>
        <span className={`status status-${item.status}`}>{item.status}</span>
      </header>

      <dl>
        <dt>명칭</dt>
        <dd>{item.name}</dd>

        <dt>부호</dt>
        <dd>{item.symbol}</dd>

        <dt>페이지</dt>
        <dd>{item.page}</dd>

        <dt>카드번호</dt>
        <dd>{item.cardIndex}</dd>
      </dl>

      <div className="actions">
        <button type="button">수정 저장</button>
        <button type="button">확인완료</button>
        <button type="button">보류</button>
        <button type="button">제외</button>
      </div>
    </section>
  );
}
